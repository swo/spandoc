Spandoc: Smart Pandoc
=====================

Read the documentation (at read the docs?)

Get started
-----------

To do
-----

- Add a `--watch` option using Watchdog
- Fix the streaming input
- Move the tempfile stuff out of the `__main__` block
